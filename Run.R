# conn <- chariot::connectAthena()
#
# # Drop current schema
# clearSchema(conn = conn,
#             targetSchema = "public",
#             cascade = TRUE)
#
#
# ddl(conn = conn)

# chariot::dcAthena(conn = conn,
#                   remove = TRUE)
